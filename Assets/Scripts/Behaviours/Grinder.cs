using UniRx;
using UnityEngine;

public class Grinder : MonoBehaviour
{
    //private void test()
    //{
    //    var x = new IObservable<bool>[]{ new BoolReactiveProperty(false), new BoolReactiveProperty(false), new BoolReactiveProperty(true) };
    //    var o = x.ToObservable();
    //    var a = o.Scan((o1, o2) => o1.Merge(o2));
    //}
}
